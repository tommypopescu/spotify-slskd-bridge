version: '3.9'
services:
  web:
    image: ghcr.io/yourorg/spotify-slskd-bridge:latest
    container_name: spotify-slskd-bridge
    ports:
      - '8080:8000'
    env_file: .env
    restart: unless-stopped
    extra_hosts:
      - 'host.docker.internal:host-gateway'
